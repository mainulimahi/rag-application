"""API router for /api/data-sources endpoints (v2 data analysis).

Placeholder — routes to be implemented in Step 2.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/data-sources", tags=["data-sources"])
