"""API router for /api/data-files endpoints (v2 data analysis).

Placeholder — routes to be implemented in Step 2.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/data-files", tags=["data-files"])
