# Backend API base URL — visible to the browser (NEXT_PUBLIC_ prefix required).
# Override in frontend/.env.local for custom local setups.
# In Docker Compose, override via the build args / environment block.
NEXT_PUBLIC_API_URL=http://localhost:8000
